const element=document.getElementById("meow")

if(element)
{
    //getElementById returns null or HTMLElement
    //in this case we check if element is null or not
    //and if we are isnide block means element was not null
    //and if we hover over element we see now only HTMLElement is coming not null
    //TS is intelligent enough to get type
    element
}
else
{
    element
}


let str:string|undefined;
if(str)
{
    str
}
else
{
    str
}
